const RegisterValidator = require('./register');
const LoginValidator = require('./login');
const updateStatusValidator = require('./updateStatus');

module.exports = {
  RegisterValidator,
  LoginValidator,
  updateStatusValidator
};
